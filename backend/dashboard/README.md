# Dashboard Module

This module provides the main dashboard summary API for the CRM system.  
It aggregates data from Customers, Leads, Deals, and other models to give a quick overview of business performance.

## API Endpoint

### GET `/api/dashboard-summary/`

**Authentication:** Required (Bearer JWT token)

**Headers:**